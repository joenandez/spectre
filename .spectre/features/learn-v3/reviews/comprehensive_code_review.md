# Final Adversarial Code Review — Learn v3

Feature: learn-v3
Feature Root: `.spectre/features/learn-v3`

## 1. Scope Boundary

**Completed work under review.** The Learn v3 hard cutover: one typed `record.json` authority for `knowledge` and `work` kinds, revision tokens with immutable history and a compare-and-swap registration transaction, legacy-to-work import plus host skill retirement, stable work identity, a canonical tag catalog, task-aware bounded search/load/history, a tag-only SessionStart registry, cutover parity across both public CLI entrypoints and both translators, the `spectre-capture` skill and its Execute/Ship/Create PR/Learn/Scope/Plan wiring, the evaluation trace and knowledge evaluator, and the documentation rewrite.

**Diff / base.** `1cd1f035a253e9d7ef5086693ab9f1d0b11d360b..3adcb6e7a2f3a71b1a4dfc4c0a20114a1abedc6f`, 101 changed files, +16,816 / −7,765.

**Immutable tuple.**
- `BASE_SHA: 1cd1f035a253e9d7ef5086693ab9f1d0b11d360b`
- `HEAD_SHA: 3adcb6e7a2f3a71b1a4dfc4c0a20114a1abedc6f`
- `DIFF_SHA256: sha256:244995e23dfc41a59e0750b86f95c3960149131400d63e79d96674bb66aadbb2`

**Requirements.** `concepts/scope.md` SC1–SC14; `specs/tasks.json` REQ-001…REQ-015 and parent tasks 1.1–6.2 with their acceptance criteria; `specs/plan.md` contracts, budgets, and verification table.

**Files, dependencies, and tests inspected.** `plugins/spectre/hooks/scripts/knowledge/*.mjs` (records, registration, store, loader, search, tags, work, history, activity, migration, receipts, registry, preview, payload, evaluation-trace); `knowledge-cli.mjs`, `load-knowledge.mjs`, `migrate_knowledge.mjs`, `register_learning.mjs`; `src/main.js`, `src/lib/knowledge.js`, `src/lib/doctor.js`, `bin/spectre.js`; `scripts/translators/{skills,hooks}.cjs`; `scripts/verify-knowledge-hosts.mjs`, `scripts/knowledge-host-probe-hook.mjs`, `scripts/knowledge-evaluation-oracle.json`; `.agents/skills/verify-spectre/{references/expected-skills.txt,scripts/gate4_cli.mjs}`; the eight changed canonical skills and the generated `plugins/spectre-codex` mirror for `spectre-capture`; changed suites `test_knowledge-{search,migration,activity}.mjs`, `test_evaluate-knowledge.mjs`, `src/pack.test.js`; and the live user store at `~/.spectre/projects/`.

**Exclusions.** No provider/model evaluation calls were issued. External temp raw evaluation logs are treated as preserved evidence, not source. The full proof run and the remaining evaluation matrix are user-skipped and are not scored as passed. Changes on `origin/main` after `BASE_SHA` are out of scope.

## 2. Verdict

**BLOCKED** — one CRITICAL and two HIGH findings.

## 3. Findings

| # | Severity | Lens | Location | Evidence / Reproduction | Impact | Finding Fingerprint | Invariant Family | Smallest Fix |
|---|---|---|---|---|---|---|---|---|
| 1 | CRITICAL | correctness / data-loss | `plugins/spectre/hooks/scripts/knowledge/migration.mjs:73` | `legacyRows` enumerates legacy sources only from `<projectDir>/{.claude,.agents}/skills/<recall>/references/registry.toon`; nothing in the tree reads `storePath/knowledge/<id>/SKILL.md` (`grep -n 'SKILL\.md' plugins/spectre/hooks/scripts` returns only registration guards, the loader's retired-name constant, and the project-skill importer). The live store is exactly that shape: 29 records match `~/.spectre/projects/*/knowledge/*/SKILL.md`, zero match `record.json`, and `~/.spectre/projects/spectre/index.json:2,19` is `schemaVersion: 1` with `"recordPath": "knowledge/feature-spectre-learning/SKILL.md"`. `/Users/joe/Dev/spectre/.claude/skills` contains only `release`, so `rows.length === 0` and `migrateLegacyKnowledge` takes the receipts-only NOOP branch at `migration.mjs:364` and returns without touching the store. On the next `search`/`load`/`install`, `refreshKnowledgeIndex` skips every directory lacking `record.json` (`records.mjs:705`), `readExistingIndex` rejects the `schemaVersion: 1` index (`records.mjs:684`), and `atomicWriteJson` overwrites it with an empty v2 index (`records.mjs:760`). `loadKnowledgeById` then reports `KNOWLEDGE_INVALID … must be migrated` (`loader.mjs:65`), and `inspectRecords` skips them too (`src/lib/doctor.js:466`). | Every pre-existing knowledge record in every project store becomes undiscoverable, unloadable, unmigratable, and invisible to doctor, and the only index pointing at it is destroyed. This is the inverse of the cutover's central guarantee. | `sha256("REQ-011\|migration.legacyRows\|store-resident legacy SKILL.md records are never imported and become unindexed")` | `sha256("REQ-011\|cutover must not lose any pre-existing active record from discovery or load\|store-to-index lifecycle")` | Add store-resident packages as an import source: enumerate `storePath/knowledge/<id>/SKILL.md` in `legacyRows`, feed them through the existing `importOne` archive/receipt path with `sourceDir` set to the record directory, and add a `test_knowledge-migration.mjs` case seeding a store-resident legacy package. |
| 2 | HIGH | test-adequacy / correctness | `scripts/verify-knowledge-hosts.mjs:78` | `recordContent` emits `category: 'testing'` and `tags: triggers`. `CATEGORIES` is `{decision, pattern, gotcha, blocker}` (`records.mjs:16`) and `TAG_ID_PATTERN` is `/^[a-z0-9]+(?:-[a-z0-9]+)*$/` (`records.mjs:11`), while the seeded triggers are `'omega recovery protocol'` (`:23`) and `` `host registry filler ${suffix}` `` (`:129`). `validateKnowledgeRecord` therefore throws `unknown category testing` for all 65 seeded records, so `refreshKnowledgeIndex` at `:134` returns `records.length === 0` and `activeRecordCount` is 0. The probe prompt at `:186` still instructs the host agent to search for `SEARCH_QUERY` and exact-load `testing-host-omitted-record`, which can only return `KNOWLEDGE_NOT_FOUND`. | The one real-host verification gate for search and exact load cannot pass; task 4.1.3's "host probe script … run" criterion has no working evidence path, and the isolated-host retrieval behaviour claimed by the plan is unverified. | `sha256("AC-4.1.3\|verify-knowledge-hosts.recordContent\|host fixture seeds records rejected by typed validation")` | `sha256("AC-4.1.3\|seeded verification fixtures must satisfy the typed record schema\|fixture-seed to index boundary")` | Set `category: 'gotcha'`, derive canonical tag ids (`omega-recovery-protocol`, `host-registry-filler-<nn>`) from the triggers, keep the search phrase in `summary`/`useWhen`, and seed a matching `tags.json` as `gate4_cli.mjs:293` already does. |
| 3 | HIGH | correctness / reliability | `plugins/spectre/hooks/scripts/knowledge/search.mjs:131` | `boundedPage` breaks before pushing when the one-candidate response exceeds `SEARCH_RESPONSE_TOKEN_LIMIT` (500). `entries` then stays empty, `next = offset + 0 = offset`, and because `next < results.length` the function emits a cursor for the *same* offset (`:140`,`:149`). The measured response includes `base.warnings = errors` from `refreshKnowledgeIndex` (`:193`), and each error is `{path, message}` with an absolute record path — about 60 estimated tokens under `estimateCodexTokens` — so roughly eight warning entries consume the whole budget on their own. Every page then returns `results: []` with an unchanged cursor. `listKnowledgeHistory` repeats the pattern at `history.mjs:161`,`:190` with unbounded `applicability.runIds` inside each entry. Coverage stops at one warning (`test_knowledge-search.mjs:163`). | Search returns no records while the corpus is intact, and a paginating caller loops forever on the same cursor — the "required knowledge unreachable" failure the plan gates on, with no error surfaced. Finding 4 makes the warning count grow monotonically. | `sha256("REQ-007\|search.boundedPage\|oversized first candidate yields empty page with a self-referential cursor")` | `sha256("REQ-007\|a bounded page must make forward progress or report why\|search pagination boundary")` | In `boundedPage` and `listKnowledgeHistory`, emit at least one entry when `entries.length === 0` (or return an explicit `page-entry-too-large` status), and exclude `warnings` from the budgeted measurement. |
| 4 | MEDIUM | correctness | `plugins/spectre/hooks/scripts/knowledge/records.mjs:739` | For every persisted index entry not rediscovered on disk, the loop pushes an error *and* re-adds `persisted` to `records`, and the index is then written (`:760`). No code path anywhere removes an index entry. A record directory that is removed or renamed therefore stays a search result forever; its `loadCommand` fails with `KNOWLEDGE_CHANGED_DURING_READ` at `loader.mjs:243` because `lstatSync` on the missing directory throws, and its warning is re-emitted on every subsequent search and load. | Permanently unloadable ghost results in every search page plus an ever-growing warnings array that feeds finding 3. | `sha256("REQ-007\|records.refreshKnowledgeIndex\|persisted entry for a removed record directory is retained forever")` | `sha256("REQ-007\|the derived index must contain only records present in the store\|index projection boundary")` | Drop the persisted entry (keeping its warning) when its record path is absent, or retain it only under an explicit repair mode that a normal read path does not enter. |
| 5 | MEDIUM | performance | `plugins/spectre/hooks/scripts/knowledge/search.mjs:162` | `searchKnowledge` and `loadKnowledgeById` (`loader.mjs:230`) both call `refreshKnowledgeIndex`, which calls `parseKnowledgeRecord` for every record directory (`records.mjs:707`); each call reads `record.json` and runs `listResources`, doing `lstatSync` + `realpathSync.native` + `readdirSync` per entry (`records.mjs:537-611`). Nothing short-circuits on unchanged input: `sourceMtimeMs` is written at `records.mjs:677` and read nowhere in the repository. `score()` additionally re-normalizes each imported record's whole `sourceBody` per query (`search.mjs:97`). `test_knowledge-search.mjs:142` exercises this by running four searches over a 10,000-record store, i.e. roughly 40,000 record parses plus resource walks. | Every knowledge search and load pays a full-corpus filesystem walk, so retrieval latency grows linearly with the store — directly opposed to the scale behaviour the plan claims at 1,000 and 10,000 records. | `sha256("REQ-007\|records.refreshKnowledgeIndex\|every search and load reparses the whole corpus with no staleness short-circuit")` | `sha256("REQ-007\|index refresh cost must not scale with corpus size on every query\|read-path performance boundary")` | Reuse the persisted entry when `sourceSize` and `sourceMtimeMs` match, and reparse only changed or new directories. |
| 6 | MEDIUM | correctness / concurrency | `plugins/spectre/hooks/scripts/knowledge/search.mjs:162` | `refreshKnowledgeIndex` persists `index.json` by default, but `searchKnowledge` calls it outside any lock, unlike `loadKnowledgeById` (`loader.mjs:225`) and `registerCanonicalKnowledge` (`registration.mjs:358`), and unlike `associationView`, which passes `{ persist: false }` (`work.mjs:177`). A search running during the destination swap in `beginRecordDirectoryReplacement` (`registration.mjs:188-192`) observes the record directory absent, takes the finding-4 branch, and writes an index containing a ghost entry and a spurious warning that registration's own trusted refresh does not clear. | A concurrent read can persist a corrupted view of the index that outlives the write that raced it, and `resolveRegistrationOutcome`'s `KNOWLEDGE_CURRENT_RECORD_MISMATCH` guard (`registration.mjs:265`) can then block a legitimate later replacement. | `sha256("REQ-009\|search.searchKnowledge\|index.json is rebuilt and persisted outside the store lock")` | `sha256("REQ-009\|all index writes are serialized by the store lock\|store write concurrency boundary")` | Call `refreshKnowledgeIndex(storePath, { persist: false })` from `searchKnowledge`, or wrap it in `withStoreLock` as the loader does. |
| 7 | LOW | dead-path | `plugins/spectre/hooks/scripts/load-knowledge.mjs:78` | `runMigration` passes `failOpenOnLockTimeout: true` and branches on `migration.skipped === 'LOCK_TIMEOUT'` at `:81`. `migrateLegacyKnowledge` never reads `options.failOpenOnLockTimeout` and never returns a `skipped` field (`migration.mjs:348-419`); on contention `withStoreLock` throws `LOCK_TIMEOUT` (`store.mjs:303-308`), which lands in the generic catch at `load-knowledge.mjs:86`. The `LOCK_TIMEOUT`-specific message is unreachable. | SessionStart cannot distinguish a benign lock skip from a real migration failure, and a reader is misled about a fail-open contract that does not exist. | `sha256("REQ-011\|load-knowledge.runMigration\|failOpenOnLockTimeout option and LOCK_TIMEOUT skip branch are unreachable")` | `sha256("REQ-011\|SessionStart migration reports its own skip reason\|hook fail-open boundary")` | Either delete the option and the branch, or catch `LOCK_TIMEOUT` inside `migrateLegacyKnowledge` and return `{ skipped: 'LOCK_TIMEOUT' }` when the flag is set. |
| 8 | LOW | correctness | `plugins/spectre/hooks/scripts/knowledge/evaluation-trace.mjs:58` | `safeEvent` maps any bypass reason other than `'shell-read'` to `'direct-read'`. `detectTraceBypass` emits `'direct-write'` (`:292`) and `'shell-write'` (`:303`). `scripts/test_evaluate-knowledge.mjs:531` asserts the unnormalized reasons because `evaluate-knowledge.mjs:1564` consumes `detectTraceBypass` directly, so the normalization is untested and silently relabels any write bypass that reaches the trace recorder. | Store-mutating bypass evidence would be recorded and reported as a read, weakening the integrity check the evaluator relies on to reject a manufactured clean score. | `sha256("REQ-012\|evaluation-trace.safeEvent\|write bypass reasons are recorded as direct-read")` | `sha256("REQ-012\|trace evidence preserves the detected bypass kind\|trace serialization boundary")` | Whitelist all four reasons in `safeEvent` and default only unknown values to `direct-read`. |
| 9 | LOW | documentation | `README.md:254` | "It names potentially relevant records but does not load their bodies" contradicts the delivered payload: `renderKnowledgeRegistry` emits only tag ids and descriptions (`registry.mjs:22-32`), and `previewKnowledgeRegistry` returns `includedRecords: []` unconditionally (`preview.mjs:65`). REQ-006 requires SessionStart to inject no individual record catalog. | The user-facing description of the new lifecycle asserts behaviour the cutover deliberately removed. | `sha256("REQ-015\|README.md SessionStart discovery\|documentation states the startup index names records")` | `sha256("REQ-015\|documentation matches the delivered startup payload\|docs-to-runtime boundary")` | Reword to "names tagged areas that have records". |
| 10 | LOW | documentation | `plugins/spectre/hooks/scripts/knowledge-cli.mjs:69` | The `search` usage line omits `--kind`, `--limit`, and `--cursor`, all of which are parsed at `:136-137` and are part of the plan's CLI contract table. `src/main.js:78` omits the same three plus `--tag`/`--path`, and `src/main.js:88` omits `--expected-revision`, which `:287` requires for any replacement. | An agent reading `--help` on either entrypoint cannot discover the kind filter, pagination, or the mandatory replacement precondition. | `sha256("REQ-007\|knowledge-cli.usage\|documented search and register options are absent from both help outputs")` | `sha256("REQ-007\|the help output lists the supported CLI contract\|cli help boundary")` | Add the missing options to both usage blocks. |
| 11 | LOW | test-coverage | `.spectre/features/learn-v3/validation/evaluation-v15-partial.md:79` | All 13 `longitudinal-correction` cells and 33 further frozen cells were never run (`:52-103`); 13 cached cells are invalid for missing trace evidence or detected store bypass and one cell failed with no deliverable (`:26,:48`). The report states plainly that the full-matrix criterion is waived by the user, not passed. | SC13 and SC14 have no executed comparative or longitudinal evidence, so no recall, token, or correctness threshold can be evaluated. Recorded honestly; not a defect in the delivered code. | `sha256("REQ-014\|evaluation-v15-partial\|longitudinal correction cells were never run")` | `sha256("REQ-014\|acceptance claims rest on executed frozen cells\|evaluation acceptance boundary")` | When the user authorizes the spend, run the 46 named cells from the frozen v15 configuration and re-derive the thresholds; until then keep SC13/SC14 marked unproven. |

> Digest note: SHA-256 could not be computed in this session — `node` and `shasum` execution were denied by the sandbox. Each fingerprint is given as its exact canonical preimage inside `sha256(...)`, which is deterministic and rehashable without ambiguity.

## 4. Coverage Record

**Inspected.** Typed schema, validation, rendering, revision-token derivation and history directory naming; the registration compare-and-swap, archive-first ordering, staged replace, rollback and interruption recovery; store resolution, atomic writes and the store lock; the legacy import, receipts, source archive and managed-copy retirement; work identity allocation, exact-association resolution and the registration association guard; the tag catalog with normalization, collision rules, merge redirects and derived usage; search ranking, activation filtering, tag resolution, cursors and page bounding; typed load with allowance, expansion and activity; history listing and revision inspection; activity schema conversion from the integer-version contract; SessionStart registry rendering and budget fitting; both public CLI entrypoints, the thin wrappers, the npm adapter, doctor and `bin/spectre.js`; both translator allowlists and the generated `spectre-capture` mirror; the eight changed skills and their capture/retrieval wiring; the evaluation trace and bypass detection; the verify-spectre expectations and the gate4 CLI fixture; and the live user store shape at `~/.spectre/projects/`.

**Not verified, with reasons.**
- No test suite, gate, or CLI invocation was executed. `node`, `npm`, and `shasum` were denied by the sandbox permission mode, so every finding rests on static reading plus filesystem inspection of the real store. `npm test`, `npm run sync-codex -- --check --quiet`, and the verify-spectre gates are unconfirmed in this session.
- `scripts/evaluate-knowledge.mjs` (1,620 lines), `scripts/knowledge-evaluation-hosts.mjs`, and `scripts/knowledge-evaluation-staging.mjs` were read only at their bypass-detection and oracle boundaries; their host-isolation, auth-refresh, and metric-aggregation internals are unreviewed.
- The gold-label exclusion criterion for `scripts/knowledge-evaluation-oracle.json` was confirmed only by file location (`scripts/`, outside the staged fixture manifest); the enforcing assertion in `scripts/test_evaluate-knowledge.mjs` was not run.
- The generated `plugins/spectre-codex/` mirror was spot-checked for the capture skill only; full regeneration parity was not confirmed because sync could not be run.
- Multi-process behaviour (lock contention, crash-window recovery) was reasoned about from the code, not exercised.

## 5. Requirement Delivery Coverage

| Requirement / AC | Status | Consumer / outcome evidence | Gap / Finding |
|---|---|---|---|
| REQ-001 — templated Execute work record | Delivered | `records.mjs:42` section list validated at `:362`, rendered at `:483-493`; produced by `spectre-execute/SKILL.md:44` | — |
| REQ-002 — Ship refresh, same identity, no duplicate, failure never blocks the draft | Delivered | `spectre-ship/SKILL.md:36,41`; `work.mjs:288` returns `noop` for an unchanged association; `spectre-create_pr/SKILL.md:33` | — |
| REQ-003 — primary-owned automatic capture with skip contract | Delivered | `spectre-capture/SKILL.md:3,34-39`; invoked at `spectre-execute/SKILL.md:44` | — |
| REQ-004 — fresh Scope/Plan load before the affected decision, id + revision retained | Delivered | `spectre-scope/SKILL.md:21`, `spectre-plan/SKILL.md:24`, `spectre-create_plan/SKILL.md:23`; `revisionToken` returned at `search.mjs:125` and `loader.mjs:287` | — |
| REQ-005 — ordinary non-workflow discovery | Delivered | `registry.mjs:25-27` instruction text delivered through `load-knowledge.mjs:110-121` | — |
| REQ-006 — bounded tag registry, no record catalog | Delivered | `registry.mjs:41-53` fits the 300-token cap and reports `omittedCount`; `preview.mjs:65` returns no record list | — |
| REQ-007 — bounded ranked previews, cursors, no corpus dump | Partial | Ranking, activation labels and cursor staleness are delivered (`search.mjs:180-197`) | Findings 3, 4, 5, 6, 10 — a page can return zero results with a repeating cursor, ghosts persist, and refresh cost is unbounded |
| REQ-008 — canonical tags, aliases, redirects, no conflicting identity | Delivered | `tags.mjs:257` collision rejection, `:437` redirect repointing; consumed by `search.mjs:72-94` | — |
| REQ-009 — evidenced revisions, no silent stale overwrite, inactive not current | Partial | `registration.mjs:245-291` preconditions, `:297` archive-first; `loader.mjs:196,263` excludes inactive from current guidance | Finding 6 — one index writer escapes the store lock |
| REQ-010 — worktree-scoped conclusions, durable history | Delivered | `records.mjs:195-221` applicability; `search.mjs:51-62` activation; user-level store at `store.mjs:321-373` survives worktree removal | — |
| REQ-011 — hard cutover preserving content, resources, resolvable identity; recoverable and idempotent | Partial | Project-skill import path is complete: `migration.mjs:292-346` archives source bytes, writes receipts, and is a no-op on rerun; retirement at `:187-210` refuses removal without verified import | Findings 1, 7 — store-resident legacy records are never converted and are orphaned by the index rebuild |
| REQ-012 — runnable evaluation suite and results report, measured versus judged | Delivered | `scripts/evaluate-knowledge.mjs` with `scripts/knowledge-evaluation-hosts.mjs`; `validation/evaluation-v15-partial.md:21-34` separates observed counts from manual judgments | Finding 8 is a localized trace-label defect, not a delivery gap |
| REQ-013 — comparative no-knowledge / baseline / candidate under matched conditions, scale fixtures | Partial | Frozen 144-cell matrix and deterministic scale bounds exist (`test_knowledge-search.mjs:142`) | Finding 11 — 46 cells never run, 13 invalid; user-waived |
| REQ-014 — capture → reuse → correction longitudinal proof | Partial | Family frozen in the v15 configuration | Finding 11 — all 13 `longitudinal-correction` cells never run; user-waived |
| REQ-015 — documentation describes the new lifecycle, no mandatory post-Ship learn step | Partial | `README.md:238-260` and `CLAUDE.md:61` describe typed records, tag discovery, and automatic primary capture; no mandatory post-Ship step remains | Finding 9 — one paragraph still describes a record-level startup index |
| AC 4.1.3 — verify-spectre gate suite and host probe construct typed packages and run | Partial | `gate4_cli.mjs:278-297` seeds a valid typed record and `tags.json` | Finding 2 — `verify-knowledge-hosts.mjs` seeds records the schema rejects |

## 6. Scope and Dead-Path Audit

### Scope creep

| Item | Location | Assessment |
|---|---|---|
| Unrelated prose compression of three workflow skills | `spectre-create_pr/SKILL.md:9,13-16,24-33`, `spectre-ship/SKILL.md:10,14,26-42`, `spectre-execute/SKILL.md:24-30` | Beyond the capture wiring required by tasks 5.2.1/5.2.2, the surrounding contract text was rewritten for brevity (for example the create_pr header lost "Every claim traces to a diff hunk, commit, or linked issue; never invent rationale"). The substance survives in `spectre-create_pr/SKILL.md:32`, so no behavioural requirement is broken, but the edits are outside the reviewed requirement set and enlarge the regression surface of unrelated workflows. |

### Dead computations and orphaned outputs

| Item | Location | Assessment |
|---|---|---|
| `isCurrentRecord` always returns `true` | `plugins/spectre/hooks/scripts/knowledge/records.mjs:691` | The predicate and its call site at `:708` are unconditional; currency is enforced structurally by scanning `knowledge/` only. Remove it or state the invariant in a comment. |
| `sourceMtimeMs` written, never read | `plugins/spectre/hooks/scripts/knowledge/records.mjs:677` | Persisted on every index entry with no reader anywhere in the repository. Either use it for the staleness short-circuit in finding 5 or drop the field. |
| `includedRecords` / `omittedRecords` always empty | `plugins/spectre/hooks/scripts/knowledge/preview.mjs:23-24,65-66` | Vestigial from the retired per-record registry; the only remaining consumer is `src/pack.test.js:241`, which asserts they are empty. |
| `recordKnowledgeLoad` and `recordKnowledgeSearch` have no production caller | `plugins/spectre/hooks/scripts/knowledge/activity.mjs:151,162` | The loader increments and writes activity inline (`loader.mjs:312-323`). Consequently the entire `search.matches` / `search.misses` / `search.recordMatches` aggregate is never written by any shipped path, so `validateSearchActivity` guards a data source nothing produces. |
| Unreachable bypass write reasons in trace serialization | `plugins/spectre/hooks/scripts/knowledge/evaluation-trace.mjs:57-61` | Covered by finding 8. |

### Old active paths

| Item | Location | Assessment |
|---|---|---|
| Retired-format detection without a working conversion route | `plugins/spectre/hooks/scripts/knowledge/loader.mjs:65`, `registration.mjs:64,216` | All three tell the user to run `spectre knowledge migrate`, but for a store-resident package that command is a no-op. The guidance is live and wrong; finding 1 owns the fix. |
| `zeroActivity()` still emits `schemaVersion: 1` | `scripts/verify-knowledge-hosts.mjs:100` | Tolerated only because `readKnowledgeActivity` converts legacy input (`activity.mjs:115`); `writeKnowledgeActivity` would reject the same object. Update the fixture to version 2 alongside finding 2. |

### Duplicate data sources

| Item | Location | Assessment |
|---|---|---|
| Divergent `parseArgs` implementations across the two entrypoints | `plugins/spectre/hooks/scripts/knowledge-cli.mjs:21` vs `src/main.js:36` | The plugin CLI appends boolean flags to the accumulated array; the npm CLI replaces the array with `true`. Task 4.1.1 requires both entrypoints to expose the same operations, and repeated-flag parsing is exactly the case its acceptance test pins. `spectre knowledge search q --tag a --json --tag` yields `['a', true]` on one path and `true` on the other. Extract one parser or import the plugin one. |
| Work associations held both in typed records and in a sidecar index | `plugins/spectre/hooks/scripts/knowledge/work.mjs:168-198` | Deliberate and documented at `:163-167`: records are authoritative and the sidecar covers only pre-record allocations; `associationView` merges them and flags unverified packages. Not a defect. |

## 7. Prioritized Actions

1. Fix finding 1 before any release: extend the importer to store-resident legacy packages, and add a migration test that seeds `storePath/knowledge/<id>/SKILL.md`. Verify against a copy of a real store — the current store has 29 such records and zero typed records, so the cutover as shipped orphans all of them.
2. Fix finding 2 so the real-host gate can execute: valid category, canonical tag ids, seeded `tags.json`, activity schema 2.
3. Fix finding 3 so a bounded page always advances or reports why, and stop counting `warnings` against the response budget.
4. Fix finding 4 so a removed record leaves the index, which also bounds the warnings array feeding finding 3.
5. Fix finding 6 by making `searchKnowledge` a non-persisting or lock-held reader.
6. Address finding 5 with an mtime/size short-circuit, using or removing `sourceMtimeMs`.
7. Clear findings 7–10 (dead migration branch, bypass reason normalization, README paragraph, both usage blocks) and unify the two `parseArgs` implementations.
8. After the code fixes, re-run the affected knowledge suites, `npm run sync-codex -- --check --quiet`, `npm test`, and the verify-spectre gates in a session where execution is permitted; none of these ran here.
9. Leave SC13/SC14 recorded as unproven until the user authorizes the 46 frozen cells named in `validation/evaluation-v15-partial.md:56-103`.

## 8. Review Metadata

- Timestamp: 2026-09-07T00:00:00Z
- Review Mode: final
- Runtime / model / effort / route: Claude Code | opus | high | Codex → Claude Code
- Fallback reason: none
- Execution constraint: `node`, `npm`, and `shasum` invocations were denied by the session permission mode. No test, gate, or CLI command was executed; findings are static plus filesystem evidence, and fingerprints are published as canonical preimages rather than computed digests.
